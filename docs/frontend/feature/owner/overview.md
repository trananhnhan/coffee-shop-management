/owner/dashboard    → statistics (live + static)
/owner/branches     → list, so sánh, click vào => drill-down
/owner/orders        → lịch sử, filter branch
/owner/staff         → nhân viên, filter branch
/owner/inventory     → tồn kho, filter branch
/owner/branches/:id  → (optional, nếu kịp) chi tiết sâu 1 branch